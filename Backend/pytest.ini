[pytest]
# Register custom markers to avoid warnings
markers =
    integration: tests that require external services or database connection (skip in CI by default)

# Test discovery settings
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
